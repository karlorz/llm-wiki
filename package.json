{
  "name": "skillwiki-monorepo",
  "private": true,
  "version": "0.10.11",
  "workspaces": [
    "packages/shared",
    "packages/cli",
    "packages/skills",
    "packages/agent-memory-trends",
    "packages/skillwiki-maintenance"
  ],
  "scripts": {
    "build": "npm run -ws --if-present build",
    "test": "npm run -ws --if-present test",
    "test:vault-sync": "bash -c 'for t in packages/vault-sync/test/*.test.sh; do echo \"=== $t ===\"; bash \"$t\" || exit 1; done'",
    "lint": "npm run -ws --if-present lint",
    "bump": "bash scripts/bump-version.sh",
    "release": "bash scripts/release.sh",
    "pack:cli": "bash scripts/pack-cli.sh",
    "materialize:plugins": "bash scripts/materialize-plugin-assets.sh",
    "materialize:plugins:check": "bash scripts/materialize-plugin-assets.sh --check",
    "clean": "rm -rf packages/*/dist packages/*/.tsbuildinfo artifacts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1045.0"
  }
}
