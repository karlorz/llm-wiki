{
  "name": "skillwiki-monorepo",
  "private": true,
  "version": "0.10.32",
  "workspaces": [
    "packages/shared",
    "packages/cli",
    "packages/skills",
    "packages/agent-memory-trends",
    "packages/skillwiki-maintenance"
  ],
  "scripts": {
    "build": "npm run -ws --if-present build",
    "test": "npm run -ws --if-present test",
    "test:release-lockfile": "bash scripts/test/release-lockfile.test.sh",
    "test:plugin-metadata": "bash scripts/test/plugin-metadata.test.sh",
    "test:vault-sync": "bash -c 'for t in packages/vault-sync/test/*.test.sh; do echo \"=== $t ===\"; env -u WIKI_REMOTE -u VS_SNAPSHOT_PROFILE -u SNAPSHOT_PROFILE -u VS_CHECK_SNAPSHOTTER bash \"$t\" || exit 1; done'",
    "test:vault-sync-live-adapter": "bash packages/vault-sync/test/snapshot-health-live-adapter.test.sh && npm run -w skillwiki test -- test/commands/snapshot-health-parity.test.ts",
    "generate:systemd-catalog": "node packages/vault-sync/scripts/generate-systemd-property-catalog.mjs",
    "check:systemd-catalog": "node packages/vault-sync/scripts/generate-systemd-property-catalog.mjs --check",
    "lint": "npm run -ws --if-present lint",
    "bump": "bash scripts/bump-version.sh",
    "release": "bash scripts/release.sh",
    "pack:cli": "bash scripts/pack-cli.sh",
    "materialize:plugins": "bash scripts/materialize-plugin-assets.sh",
    "materialize:plugins:check": "bash scripts/materialize-plugin-assets.sh --check",
    "install:activation": "bash scripts/install-skillwiki-activation.sh",
    "install:activation:check": "bash scripts/install-skillwiki-activation.sh --check",
    "clean": "rm -rf packages/*/dist packages/*/.tsbuildinfo artifacts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1045.0"
  }
}
