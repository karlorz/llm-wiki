# v0.10.73

Patch. Frozen-leaf Phase 5 skill contract so agents refuse git close of wiki work.

- `wiki-sync`, `proj-work`, `using-skillwiki`, `skillwiki-mcp`: if `.WIKI_GIT_FROZEN` exists, fail closed. No `git add`/`commit`/`push`. Captures stay HTTP MCP. Work-item close waits unpublished Tier 2 (`wiki_workitem_write` / `wiki_page_publish`).
- Test: `scripts/test/frozen-leaf-skill-contract.test.sh`.
- Does **not** bump the sg01 `skillwiki-mcp` daemon. Does **not** ship Tier 2 tools.
