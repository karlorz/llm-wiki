# v0.10.86

Patch. Accept store-ahead `log.md` during the pre-sync snapshot wait so MCP appends cannot stall GitHub promotion before rclone starts.

- Pre-sync wait still requires S3 `index.md` to match the freeze. If S3 `log.md` is a strict byte-prefix append of the freeze, snapshot adopts those store bytes and continues.
- Keeps the v0.10.85 post-sync refresh and worktree==current-store gate. Divergent or shorter S3 log still times out.
- Excludes `.superpowers/**` from rclone sync and the S3-only-note preflight. Does not raise the 200-note cap.

Does **not** bump the sg01 `skillwiki-mcp` daemon. Requires an attended vault-sync snapshotter reinstall on sg01.
