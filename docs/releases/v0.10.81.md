# v0.10.81

Patch. Fix `wiki_query` `scope=all` ranking so Layer-3 work is not drowned by typed source-overlap.

- `scope=all` ranks work and typed in separate pools, then zip-merges work-first. Default `typed` ranking is unchanged.
- Work paths get a path-segment keyword bonus for exact non-generic segments (for example `llm-wiki`). Typed pages do not.
- Live acceptance: `scope=all` `"llm-wiki open work HTTP MCP doctor"` must include a `projects/llm-wiki/work/` hit in the top 5 and must not rank `queries/2026-07-04-knowledge-monetization-strategy.md` first.

Does bump the sg01 `skillwiki-mcp` daemon. Operator-gated pin after CI green.
