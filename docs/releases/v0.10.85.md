# v0.10.85

Patch. Re-freeze snapshot projection expectations from current S3 after rclone sync so a mid-sync MCP `log.md` append no longer blocks GitHub promotion.

- After rclone sync (and the post-repair sync), `wiki-snapshot` re-reads store `index.md` / `log.md` in parallel, refreshes the frozen expected bytes/hashes, then runs the existing worktree + semantic preview gates.
- The pre-sync freeze and direct-store wait stay in place. A stale worktree that does not match current store still fails closed.
- Adds a newer-log-during-sync fixture. Leaves the 200-note `direct-S3-not-git` cap unchanged.

Does **not** bump the sg01 `skillwiki-mcp` daemon. Does require an attended vault-sync snapshotter reinstall on sg01 so the live `wiki-snapshot.sh` picks up the race fix.
