# v0.10.82

Patch. Treat fetch-only leaves as healthy in `skillwiki doctor` when push is disabled.

- Fetch-only leaf hosts (pull mirror, no push authority) no longer fail the `vault_sync` doctor probe; the fetch-only profile reports healthy when push is intentionally disabled.
- Materialize the `using-skillwiki` fetch-only copy so leaf installs carry the right guidance.
- Shipped from PR #75 (`doctor-fetch-only-onto-main`). Note: the `v0.10.82` tag was cut at the version-bump commit `8d36dbc` and does not include the `wiki_query` structural-seed-gating fix from PR #76, which merged to main minutes later and ships in v0.10.83.

Does bump the sg01 `skillwiki-mcp` daemon. Operator-gated pin after CI green.
