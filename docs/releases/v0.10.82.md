# v0.10.82

Patch. Gate `wiki_query` structural seeds in work/all scopes so weak-seed packet clusters cannot outrank strong direct matches.

- On large vaults, generic query terms turn thousands of pages into weak keyword seeds; work-item families sharing identical source lists (July playground packets) self-reinforced via source-overlap sums (e.g. 108 shared sources x 4 = 432) and drowned strong direct matches (September doctor spec: keyword 22, composite 44).
- `scope=work` and the work pool of `scope=all` now restrict structural signals (source overlap, wikilinks, Adamic-Adar) to the top 20 keyword seeds. Weak-seed clusters get no structural boost.
- Default `typed` ranking and the typed pool of `scope=all` are unchanged.
- Real-vault verification: `scope=work` `"llm-wiki open work HTTP MCP doctor"` top 6 are all `projects/llm-wiki/work/` September HTTP MCP items; the 2026-07 playground packets and the 2026-07-14 delete-intent spec no longer lead on overlap inflation.

Does bump the sg01 `skillwiki-mcp` daemon. Operator-gated pin after CI green.
