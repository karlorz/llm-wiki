# v0.10.74

Patch. Tier 2 HTTP MCP overwrite tools so frozen-leaf agents can close wiki work after an attended daemon bump.

- `wiki_workitem_write`: create/overwrite allowlisted `projects/*/work/**` markdown and `projects/*/knowledge.md`. Overwrite requires `base_sha256` of last-read file bytes (`FILE_CHANGED` on mismatch).
- `wiki_page_publish`: create/overwrite allowlisted Layer-2 pages (`entities|concepts|comparisons|queries|meta`). Same CAS. Path check matches CLI `validateTypedTarget`. Does **not** spawn `skillwiki page publish` (sg01 snapshotter managed-write would fail); writes use the daemon S3+FUSE path.
- Frozen-leaf skills (`proj-work`, `skillwiki-mcp`, `using-skillwiki`, `wiki-sync`): feature-detect the tools. If advertised, mutate via MCP CAS. If absent, `wiki_capture` or STOP. Git remains fail-closed; GitHub is `wiki-snapshot`.
- Tests: mcp-server allowlist + CAS; frozen-leaf skill contract.

Does **not** bump the sg01 `skillwiki-mcp` daemon. Live close-work still waits for that attended restart.
