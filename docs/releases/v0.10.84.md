# v0.10.84

Patch. Simplify pass over the v0.10.83 structural-seed gate; ranking output is byte-identical.

- `rankPages` builds one gated structural seed list instead of three seed sets; `scoreSourceOverlap` iterates the seed pages directly instead of rescanning every candidate page per candidate (O(pages x seeds) instead of O(pages^2); with the top-20 gate that is ~60k instead of ~9M iterations at 3k work items).
- Comments document that top-K gating intentionally runs before historical-cycle exclusion, and name per-family overlap bounding inside `scoreSourceOverlap` as the long-term unification target for the gate and the 2026-08-04 guardrail.
- Verified byte-identical `wiki_query` output vs v0.10.83 on the real vault (~9.5k pages) for `scope=work`, `scope=all`, and default typed. CLI + mcp-server suites: 2252/2252.

Behavior-identical to v0.10.83; sg01 re-pin optional. Operator-gated as always.
