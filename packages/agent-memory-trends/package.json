{
  "name": "@skillwiki/agent-memory-trends",
  "version": "0.9.6",
  "private": true,
  "type": "module",
  "bin": {
    "agent-memory-trends": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "doctor": "npm run --silent build && node dist/cli.js doctor",
    "collect": "npm run --silent build && node dist/cli.js collect",
    "daily": "npm run --silent build && node dist/cli.js daily",
    "publish": "npm run --silent build && node dist/cli.js publish",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.12.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
